name: Deploy GitHub Pages

on:
  push:
    branches: [main, master]
    paths:
      - "web/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build static site
        run: |
          set -euo pipefail
          mkdir -p _site

          # Public HTML
          for file in index.html 404.html login.html callback.html dashboard.html; do
            cp "web/$file" "_site/$file"
          done

          # Assets
          cp -r web/css web/js web/static _site/
          cp -r web/login web/callback web/dashboard _site/

          # Downloads (exe only — no Python bundle)
          mkdir -p _site/downloads
          if [ -f web/downloads/dotx-pc-check.exe ]; then
            cp web/downloads/dotx-pc-check.exe _site/downloads/
          fi

          # GitHub Pages: skip Jekyll processing
          cp web/.nojekyll _site/.nojekyll

          echo "Site files:"
          find _site -type f | head -50

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
