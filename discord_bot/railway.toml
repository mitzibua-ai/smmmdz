[build]
builder = "NIXPACKS"
buildCommand = "pip install -r ../requirements.txt"

[deploy]
# Optional / unused — bot runs on your PC with Supabase. Prefer watch-bot.bat.
startCommand = "python ../run_dotx.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
